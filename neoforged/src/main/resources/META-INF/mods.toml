modLoader="javafml"
loaderVersion="[47,)"
license="MIT License"
issueTrackerURL = "https://github.com/XenFork/tXenAllLanguageMod/issues"

[[mods]]
   modId="txenalllanguagemod"
   version="1.0.0.0"
   displayName="next all language mod"
   displayURL="https://github.com/XenFork/tXenAllLanguageMod"
   logoFile="icon.png"
   credits=""
   authors="XenFork Union"
   description='''

   '''

[[dependencies.txenalllanguagemod]] #optional
# the modid of the dependency
   modId="forge" #mandatory
# Does this dependency have to exist - if not, ordering below must be specified
   mandatory=true #mandatory
# The version range of the dependency
   versionRange="[47,)" #mandatory
# An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
   ordering="NONE"
# Side this dependency is applied on - BOTH, CLIENT or SERVER
   side="BOTH"
# Here's another dependency
[[dependencies.txenalllanguagemod]]
   modId="minecraft"
   mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
   versionRange="[1.20.1,)"
   ordering="NONE"
   side="BOTH"